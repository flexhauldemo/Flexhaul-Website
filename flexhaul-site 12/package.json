{
  "name": "flexhaul-site-functions",
  "private": true,
  "description": "Serverless functions powering SMS confirmations and reminders for the FlexHaul & Demolition website. The website itself is plain static HTML/CSS/JS and needs no build step — this file only exists so Netlify can install the two packages the functions in netlify/functions use.",
  "dependencies": {
    "twilio": "^5.3.0",
    "@netlify/blobs": "^8.1.0"
  }
}
